# Configuracion con datos reales (IPs, puertos)
config/env

# Secreto compartido de mlvpn
keys/

# Configuraciones generadas y logs
generated/

# Compilacion de mlvpn
build/

# macOS
.DS_Store
.DS_Store?

# Playwright (screenshots y logs de automatización)
.playwright-mcp/

# Imágenes y capturas de trabajo (excepción: las del README en docs/screenshots/)
*.png
*.jpg
!docs/screenshots/*.png
!docs/screenshots/*.jpg

# MCP config local
.mcp.json

# pre-commit
.secrets.baseline

# Python
__pycache__/
*.pyc

# Reportes JUnit XML generados por tests/test_REQ-*.sh
reports/

# REQ-NET-33 P1 coverage runtime artifact (regenerated by pytest --cov)
.coverage

# Session logs / evidence (manual artifacts, NO formal IDLC evidence —
# IDLC evidence vive en tests/ + reports/). Convención adoptada
# 2026-06-03 tras user feedback "no se muy bien lo que has hecho [con
# evidence]". Si necesitas commit de un session log concreto, hacerlo
# vía docs/v2-ubond/ con estructura formal.
evidence/
