# Python dev dependencies para ave-vpc.
# REQ-NET-33 Phase 1 — code coverage Python (08-monitor.py).
#
# Pin a versiones específicas (IDLC v6 R7: deterministic versioning).
# Versiones reflejan lo instalado en el entorno del usuario 2026-06-03;
# bump cuando haya razón concreta (CVE, feature requerida, no por inercia).

pytest==9.1.1
pytest-cov==7.1.0
coverage==7.15.0
